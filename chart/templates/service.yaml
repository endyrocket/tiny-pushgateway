apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiny-pushgateway.fullname" . }}
  labels:
    {{- include "tiny-pushgateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "tiny-pushgateway.selectorLabels" . | nindent 4 }}
